import React from 'react'
import '../App.css';

const Header = (props) => {
    return (
        <div className='App-header'>
            <h1 className="header"> JSON Beautifier</h1>
            <p class="header-desc"> A platform to format and validate JSON text. Built using React.</p>
        </div>
    )
}

export default Header
